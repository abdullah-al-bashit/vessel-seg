# PyTorch — install separately with the CUDA wheel that matches your system:
#   pip install torch==2.5.1 torchvision==0.20.1 --index-url https://download.pytorch.org/whl/cu121
# CPU-only fallback:
#   pip install torch torchvision
torch>=2.5.1
torchvision>=0.20.1

# AttentionUNet backbone (ResNet34 pretrained weights)
timm>=1.0.0

# Image I/O and processing
tifffile
scikit-image>=0.26
opencv-python-headless
scipy>=1.17

# Training utilities
scikit-learn>=1.0
numpy>=2.0
tqdm
matplotlib
pyyaml

# Experiment tracking
wandb
